#!/usr/bin/env ruby

ENV["BUNDLE_GEMFILE"] ||= File.expand_path("../Gemfile", __dir__)
require "bundler/setup"

Dir.chdir(File.expand_path("..", __dir__)) do
  Process.exec "bundle exec falcon host service.rb"
end
